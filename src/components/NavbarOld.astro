---
import siteData from '../data/site.json'

const pn = Astro.url.pathname
const isHome = pn === '/'
const site = Astro.site
const abtLink = isHome
  ? '#about'
  : `${site}#about`
const stepsLink = isHome
  ? '#steps'
  : `${site}#steps`
const ftLink = isHome
  ? '#features'
  : `${site}#features`
const regLink = isHome
  ? '#register'
  : `${site}#register`
---
<nav class="flex flex-col sm:flex-row w-full justify-between items-center px-4 sm:px-6 py-1 bg-main-blue shadow sm:shadow-none nav-bar">
  <div class="w-full sm:w-auto self-start sm:self-center flex flex-row sm:flex-none flex-no-wrap justify-between items-center">
    <a href={Astro.site} class="no-underline">
      <img src={`${site}img/denly_main.png`} alt="Denly Logo" class="h-image py-1">
    </a>
    <button id="menuBtn" class="hamburger block sm:hidden focus:outline-none" type="button" onclick="navToggle();" aria-label="Show Menu">
      <span class="hamburger__top-bun"></span>
      <span class="hamburger__bottom-bun"></span>
    </button>
  </div>
  <div id="menu" class="w-full sm:w-auto self-end sm:self-center hidden sm:flex flex-col sm:flex-row items-center h-full py-1 pb-4 sm:py-0 sm:pb-0">
    <a class="text-white font-bold hover:text-amber-400 text-lg text-center w-full no-underline sm:w-auto sm:pr-4 py-2 sm:py-1" href={abtLink}>About</a>
    <a class="text-white font-bold hover:text-amber-400 text-lg text-center w-full no-underline sm:w-auto sm:px-4 py-2 sm:py-1" href={stepsLink}>How it works</a>
    <a class="text-white font-bold hover:text-amber-400 text-lg text-center w-full no-underline sm:w-auto sm:px-4 py-2 sm:py-1" href={ftLink}>Why us</a>
    <a class="text-white font-bold hover:text-amber-400 text-lg text-center w-full no-underline sm:w-auto sm:px-4 py-2 sm:py-1" href={regLink}>Contact us</a>
    
    <a class="text-gray-800 border border-indigo-800 text-lg bg-gray-100 rounded-full w-auto no-underline text-center sm:text-left hover:border-blue-500 hover:text-blue-500 hover:bg-white hover:shadow-md px-6 py-1 my-2 sm:my-0 sm:ml-4" href="#">Get Started</a>
  </div>
</nav>

<style>
  .h-image {
    height: 4rem;
  }

  .nav-bar {
    
  }
</style>